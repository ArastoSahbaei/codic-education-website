const profileView = '/profile'
const personalInformationView = `${profileView}/personal`
const userInformationView = `${profileView}/account`
const purchaseHistoryView = `${profileView}/purchases`
const newsLetterSubscriptionView = `${profileView}/newssubscription`

export default {
	profileView,
	personalInformationView,
	userInformationView,
	purchaseHistoryView,
	newsLetterSubscriptionView
}